export function prReviewDashboardHtml() {
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="theme-color" content="#090d14">
<title>Paseo PR Reviews</title>
<style>
:root{color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;--bg:#090d14;--panel:#111823;--panel2:#161f2c;--border:#29364a;--text:#edf3fb;--muted:#99a8bb;--accent:#58a6ff;--good:#3fb950;--warn:#d29922;--bad:#f85149;--purple:#bc8cff}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top left,rgba(188,140,255,.09),transparent 34rem),var(--bg);color:var(--text)}a{color:var(--accent);text-decoration:none}button,input,select,textarea{font:inherit}button{border:1px solid transparent;border-radius:8px;padding:8px 12px;min-height:38px;background:#238636;color:white;font-weight:750;cursor:pointer}button.secondary{background:var(--panel2);border-color:var(--border)}button.warning{background:#9e6a03}button.danger{background:#b62324}button:disabled{opacity:.45;cursor:not-allowed}.shell{max-width:1480px;margin:auto;padding:20px}.header{position:sticky;top:0;z-index:5;background:rgba(9,13,20,.94);backdrop-filter:blur(14px);padding:14px 0;border-bottom:1px solid var(--border)}.row,.actions,.chips{display:flex;gap:8px;align-items:center;justify-content:space-between;flex-wrap:wrap}.actions,.chips{justify-content:flex-start}.header h1,.card h2{margin:0}.muted{color:var(--muted)}.chip,.badge{display:inline-flex;border:1px solid var(--border);background:var(--panel2);border-radius:999px;padding:5px 9px;color:var(--muted);font-size:12px;font-weight:720}.good{color:var(--good)!important}.warn{color:var(--warn)!important}.bad{color:var(--bad)!important}.info{color:var(--accent)!important}.grid{display:grid;gap:14px}.grid.two{grid-template-columns:repeat(2,minmax(0,1fr))}.card{background:linear-gradient(180deg,var(--panel2),var(--panel));border:1px solid var(--border);border-radius:14px;padding:16px}.section{margin-top:14px}.list{display:grid;gap:10px;margin-top:12px}.item{background:#0c121c;border:1px solid var(--border);border-radius:10px;padding:12px}.item.active{box-shadow:inset 3px 0 0 var(--purple)}.item.failed{box-shadow:inset 3px 0 0 var(--bad)}.item.fixing{box-shadow:inset 3px 0 0 var(--accent)}.title{font-weight:820}.meta,.fields{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:8px 14px;margin-top:10px;color:var(--muted);font-size:12px}.meta strong{color:var(--text)}.code{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;overflow-wrap:anywhere}.empty{padding:22px;border:1px dashed var(--border);border-radius:10px;color:var(--muted);text-align:center}.error,.finding{margin-top:9px;padding:9px;border-radius:8px;white-space:pre-wrap}.error{background:rgba(248,81,73,.08);border:1px solid rgba(248,81,73,.25);color:#ff9b96}.finding{background:rgba(188,140,255,.08);border:1px solid rgba(188,140,255,.2);color:#dbc6ff}label{display:grid;gap:6px;color:var(--muted);font-size:12px;font-weight:700}input,select,textarea{width:100%;padding:10px;border:1px solid var(--border);border-radius:8px;background:#0c121c;color:var(--text)}textarea{min-height:150px;resize:vertical}.history{max-height:520px;overflow:auto;display:grid;gap:8px}.event{border-left:2px solid var(--border);background:#0c121c;padding:9px 11px}.event time{font-size:11px;color:var(--muted)}dialog{width:min(720px,calc(100vw - 30px));border:1px solid var(--border);border-radius:14px;background:var(--panel);color:var(--text);padding:0}dialog::backdrop{background:rgba(0,0,0,.72)}.dialog-head,.dialog-body,.dialog-foot{padding:14px 16px}.dialog-head,.dialog-foot{display:flex;justify-content:space-between;gap:10px;border-bottom:1px solid var(--border)}.dialog-foot{border-top:1px solid var(--border);border-bottom:0;justify-content:flex-end}.toastbox{position:fixed;right:18px;bottom:18px;display:grid;gap:8px;z-index:20}.toast{background:var(--panel2);border:1px solid var(--border);border-radius:9px;padding:11px 13px;max-width:420px}.toast.bad{border-color:var(--bad)}@media(max-width:900px){.grid.two{grid-template-columns:1fr}.header{position:static}.shell{padding:12px}}
</style>
</head>
<body>
<div class="shell">
<header class="header">
  <div class="row"><div><h1>Serial PR Review</h1><div class="muted">Several builders, one inspector. PR review never consumes a coding slot.</div></div><div class="actions"><a href="/"><button class="secondary">Issue controller</button></a><button class="secondary" onclick="refresh()">Refresh</button><button class="secondary" onclick="post('/api/pr-reviews/reconcile')">Reconcile GitHub</button><button id="pauseQueue" class="warning" onclick="post('/api/pr-reviews/pause')">Pause reviews</button><button id="resumeQueue" onclick="post('/api/pr-reviews/resume')">Resume reviews</button></div></div>
  <div class="chips section"><span id="queueChip" class="chip">Queue</span><span id="browserChip" class="chip">Browser</span><span id="authChip" class="chip">Authentication</span><span id="conversationChip" class="chip">Conversation</span><span id="reconcileChip" class="chip">Reconciliation</span></div>
</header>

<div class="grid two section">
<section class="card"><h2>Active inspector</h2><div class="muted">Exactly one ChatGPT browser submission may be active globally.</div><div id="activeReview" class="list"><div class="empty">No active review.</div></div></section>
<section class="card"><h2>Waiting review line</h2><div class="muted">Queue order is independent from issue-coding priority.</div><div id="waitingReviews" class="list"><div class="empty">No queued reviews.</div></div></section>
</div>

<section class="card section"><div class="row"><div><h2>Managed pull requests</h2><div class="muted">Only PRs registered by Paseo are reconciled.</div></div><select id="stateFilter" onchange="renderManaged()"><option value="all">All states</option><option value="queued">Queued</option><option value="awaiting_result">Awaiting result</option><option value="fixing">Fixing</option><option value="ready_to_merge">Ready to merge</option><option value="failed">Failed</option><option value="closed_unmerged">Closed unmerged</option></select></div><div id="managedPrs" class="list"><div class="empty">No managed pull requests.</div></div></section>

<div class="grid two section">
<section class="card"><h2>Project review settings</h2><div class="fields section">
<label>Enable PR automation<select id="enabled"><option value="false">Disabled</option><option value="true">Enabled</option></select></label>
<label>Enable ChatGPT browser reviews<select id="browserEnabled"><option value="false">Disabled</option><option value="true">Enabled</option></select></label>
<label>Project conversation URL<input id="projectUrl" placeholder="https://chatgpt.com/c/..."></label>
<label>Review debounce in milliseconds<input id="debounce" type="number" min="0" max="600000"></label>
<label>Active reconciliation in milliseconds<input id="activeInterval" type="number" min="10000"></label>
<label>Idle reconciliation in milliseconds<input id="idleInterval" type="number" min="30000"></label>
<label>Maximum browser submission attempts<input id="maxAttempts" type="number" min="1" max="10"></label>
<label>Allow ChatGPT merge<select id="allowMerge"><option value="false">No</option><option value="true">Yes</option></select></label>
<label>Verify associated issue closure<select id="verifyClosure"><option value="true">Yes</option><option value="false">No</option></select></label>
<label>Allow safe Paseo issue-closure fallback<select id="closureFallback"><option value="false">No</option><option value="true">Yes</option></select></label>
</div><label class="section">Versioned review prompt template<textarea id="prompt"></textarea></label><div class="actions section"><button onclick="saveSettings()">Save settings</button></div></section>
<section class="card"><h2>Dedicated ChatGPT browser</h2><div id="browserStatus" class="meta section"></div><div class="actions section"><button onclick="post('/api/pr-reviews/browser/install')">Install Chromium</button><button class="secondary" onclick="post('/api/pr-reviews/browser/install',{withSystemDependencies:true})">Install dependencies + Chromium</button><button class="secondary" onclick="post('/api/pr-reviews/browser/open')">Launch browser</button><button class="secondary" onclick="post('/api/pr-reviews/browser/use-current',{scope:'project'})">Use current for project</button><button class="secondary" onclick="post('/api/pr-reviews/browser/use-current',{scope:'global'})">Use current globally</button><button class="secondary" onclick="post('/api/pr-reviews/browser/test')">Test destination</button><button class="secondary" onclick="post('/api/pr-reviews/browser/test',{sendTestPrompt:true})">Send harmless test</button><button class="secondary" onclick="post('/api/pr-reviews/browser/close')">Close browser</button></div><div class="error section">Reset and uninstall remove only machine-local browser state. They never expose cookies or uninstall the package.</div><div class="actions section"><button class="danger" onclick="openConfirm('Reset dedicated profile','RESET','/api/pr-reviews/browser/reset')">Reset profile</button><button class="danger" onclick="openConfirm('Uninstall browser state','UNINSTALL','/api/pr-reviews/browser/uninstall')">Uninstall browser state</button></div></section>
</div>

<section class="card section"><h2>State-transition history</h2><div class="muted">Persistent audit history across restarts.</div><div id="history" class="history section"><div class="empty">No history.</div></div></section>
</div>
<div id="toasts" class="toastbox" aria-live="polite"></div>

<dialog id="overrideDialog"><div class="dialog-head"><strong>One-time review destination</strong><button class="secondary" onclick="closeDialog('overrideDialog')">Close</button></div><div class="dialog-body"><input id="overrideManagedId" type="hidden"><label>ChatGPT conversation URL for this review only<input id="overrideUrl" placeholder="https://chatgpt.com/c/..."></label><p class="muted">This affects only the new review job. Project and global defaults remain unchanged.</p></div><div class="dialog-foot"><button class="secondary" onclick="closeDialog('overrideDialog')">Cancel</button><button onclick="submitOverride()">Queue review</button></div></dialog>
<dialog id="manualDialog"><div class="dialog-head"><strong>Manual review result</strong><button class="secondary" onclick="closeDialog('manualDialog')">Close</button></div><div class="dialog-body"><input id="manualManagedId" type="hidden"><label>Result<select id="manualResult"><option value="changes_requested">Changes requested</option><option value="approved">Approved</option></select></label><label class="section">Findings<textarea id="manualFindings"></textarea></label></div><div class="dialog-foot"><button class="secondary" onclick="closeDialog('manualDialog')">Cancel</button><button onclick="submitManual()">Record result</button></div></dialog>
<dialog id="confirmDialog"><div class="dialog-head"><strong id="confirmTitle">Confirm action</strong><button class="secondary" onclick="closeDialog('confirmDialog')">Close</button></div><div class="dialog-body"><p id="confirmText" class="muted"></p><label>Confirmation phrase<input id="confirmInput" autocomplete="off"></label></div><div class="dialog-foot"><button class="secondary" onclick="closeDialog('confirmDialog')">Cancel</button><button id="confirmButton" class="danger" disabled>Continue</button></div></dialog>

<script>
let data=null;let confirmPath=null;let confirmPhrase=null;
const e=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const short=v=>v?String(v).slice(0,9):'—';
function toast(message,bad=false){const node=document.createElement('div');node.className='toast'+(bad?' bad':'');node.textContent=message;document.getElementById('toasts').appendChild(node);setTimeout(()=>node.remove(),bad?8000:3500)}
async function api(path,options={}){const response=await fetch(path,{headers:{'content-type':'application/json'},...options});const body=await response.json();if(!response.ok)throw new Error(body.error||'Request failed');return body}
async function post(path,body={}){try{const result=await api(path,{method:'POST',body:JSON.stringify(body)});toast('Action completed.');await refresh();return result}catch(error){toast(error.message,true)}}
function closeDialog(id){const dialog=document.getElementById(id);if(dialog.open)dialog.close()}
function chip(id,text,state){const node=document.getElementById(id);node.textContent=text;node.className='chip '+state}
function badge(state){const style=['failed','closed_unmerged'].includes(state)?'bad':['fixing','submitting','awaiting_result'].includes(state)?'info':['queued','ready_to_merge','merged'].includes(state)?'good':'warn';return '<span class="badge '+style+'">'+e(state)+'</span>'}
function jobCard(job,index){if(!job)return '<div class="empty">No active review.</div>';const managed=data.managedPullRequests.find(item=>item.id===job.managedPullRequestId)||{};const controls=index==null?'':('<button class="secondary" onclick="post(\'/api/pr-reviews/move\',{reviewJobId:\''+e(job.id)+'\',direction:\'up\'})">Move up</button><button class="secondary" onclick="post(\'/api/pr-reviews/move\',{reviewJobId:\''+e(job.id)+'\',direction:\'down\'})">Move down</button><button class="danger" onclick="post(\'/api/pr-reviews/cancel\',{reviewJobId:\''+e(job.id)+'\'})">Cancel</button>');return '<div class="item '+(job.state==='submitting'?'active':'')+'"><div class="row"><div><div class="title">PR #'+e(job.pullRequestNumber)+' · '+e(managed.repository||job.repository)+'</div><div class="chips">'+badge(job.state)+'<span class="badge">SHA '+e(short(job.headSha))+'</span><span class="badge">Round '+e(job.reviewRound)+'</span></div></div><div class="actions">'+controls+'</div></div><div class="meta"><span>Request: <strong class="code">'+e(job.reviewRequestId)+'</strong></span><span>Due: <strong>'+e(job.dueAt||'—')+'</strong></span><span>Attempts: <strong>'+e(job.attempts)+'</strong></span><span>Conversation: <strong>'+e(job.conversationUrlUsed||job.conversationUrlOverride||'resolved at submission')+'</strong></span></div>'+(job.lastError?'<div class="error">'+e(job.lastError)+(job.diagnosticScreenshot?'\nDiagnostic screenshot: '+e(job.diagnosticScreenshot):'')+'</div>':'')+'</div>'}
function closedControls(record){if(record.reviewState!=='closed_unmerged')return '';const id=e(record.id);return '<div class="actions section"><button onclick="post(\'/api/pr-reviews/closed/reopen\',{managedPullRequestId:\''+id+'\'})">Reopen PR</button><button class="secondary" onclick="post(\'/api/pr-reviews/closed/return-coding\',{managedPullRequestId:\''+id+'\'})">Return to coding</button><button class="secondary" onclick="post(\'/api/pr-reviews/closed/backlog\',{managedPullRequestId:\''+id+'\'})">Return to backlog</button><button class="danger" onclick="post(\'/api/pr-reviews/closed/cancel-issue\',{managedPullRequestId:\''+id+'\'})">Cancel issue</button><button class="secondary" onclick="post(\'/api/pr-reviews/closed/manual-resolved\',{managedPullRequestId:\''+id+'\'})">Mark resolved</button></div>'}
function managedCard(record){const review=record.currentReviewJob;const fix=record.currentFixJob;const id=e(record.id);const links='<a href="'+e(record.pullRequestUrl)+'" target="_blank"><button class="secondary">Open PR</button></a>'+(record.issueUrl?'<a href="'+e(record.issueUrl)+'" target="_blank"><button class="secondary">Open issue</button></a>':'')+(record.resolvedConversationUrl?'<a href="'+e(record.resolvedConversationUrl)+'" target="_blank"><button class="secondary">Open ChatGPT</button></a>':'');const reviewControls='<button onclick="post(\'/api/pr-reviews/review-now\',{managedPullRequestId:\''+id+'\'})">Review now</button><button class="secondary" onclick="openOverride(\''+id+'\',\''+e(record.resolvedConversationUrl||'')+'\')">One-time destination</button>'+(review&&review.state==='failed'?'<button class="secondary" onclick="post(\'/api/pr-reviews/retry\',{reviewJobId:\''+e(review.id)+'\'})">Retry failed submission</button>':'')+(record.reviewState==='paused'?'<button onclick="post(\'/api/pr-reviews/resume-pr\',{managedPullRequestId:\''+id+'\'})">Resume PR</button>':'<button class="warning" onclick="post(\'/api/pr-reviews/pause-pr\',{managedPullRequestId:\''+id+'\'})">Pause PR</button>')+'<button class="secondary" onclick="openManual(\''+id+'\')">Manual result</button>';return '<div class="item '+(record.reviewState==='failed'?'failed':record.reviewState==='fixing'?'fixing':'')+'"><div class="row"><div><div class="title">PR #'+e(record.pullRequestNumber)+' · Issue #'+e(record.issueNumber)+'</div><div class="chips">'+badge(record.reviewState)+'<span class="badge">Current '+e(short(record.currentHeadSha))+'</span><span class="badge">Reviewed '+e(short(record.lastCompletedReviewSha))+'</span><span class="badge">Round '+e(record.reviewRound)+'</span></div></div><div class="actions">'+links+reviewControls+'</div></div><div class="meta"><span>Branch: <strong class="code">'+e(record.branchName)+'</strong></span><span>Last activity: <strong>'+e(record.lastActivityAt||'—')+'</strong></span><span>Last reconciliation: <strong>'+e(record.lastReconciledAt||'—')+'</strong></span><span>Conversation: <strong>'+e(record.resolvedConversationUrl||'Not configured')+'</strong></span></div>'+(review?'<div class="finding">Review job: '+e(review.state)+' · '+e(short(review.headSha))+'</div>':'')+(fix?'<div class="finding">Fix job: '+e(fix.state)+' · reviewed '+e(short(fix.reviewedHeadSha))+'<div class="actions section"><button onclick="post(\'/api/pr-reviews/send-to-coding\')">Send queued fix to coding</button></div></div>':'')+(record.lastError?'<div class="error">'+e(record.lastError)+(record.diagnosticScreenshot?'\nDiagnostic screenshot: '+e(record.diagnosticScreenshot):'')+'</div>':'')+closedControls(record)+'</div>'}
function renderManaged(){const filter=document.getElementById('stateFilter').value;const records=data.managedPullRequests.filter(record=>filter==='all'||record.reviewState===filter);document.getElementById('managedPrs').innerHTML=records.length?records.map(managedCard).join(''):'<div class="empty">No managed PRs match this filter.</div>'}
function render(){const config=data.config;const browser=data.browser;document.getElementById('activeReview').innerHTML=jobCard(data.activeReview);document.getElementById('waitingReviews').innerHTML=data.waitingReviews.length?data.waitingReviews.map(jobCard).join(''):'<div class="empty">No queued reviews.</div>';renderManaged();chip('queueChip',config.reviewQueue.paused?'Reviews paused':'Reviews running',config.reviewQueue.paused?'warn':'good');chip('browserChip',browser.library.installed?'Playwright installed':'Playwright missing',browser.library.installed?'good':'bad');chip('authChip',browser.profile.lastAuthenticatedAt?'Profile authenticated':'Authentication unverified',browser.profile.lastAuthenticatedAt?'good':'warn');const conversation=config.browserReview.projectConversationUrl||data.globalConversationUrl;chip('conversationChip',conversation?'Conversation configured':'Conversation missing',conversation?'good':'bad');chip('reconcileChip',data.runtime.lastReconciledAt?'Reconciled '+data.runtime.lastReconciledAt:'Not reconciled',data.runtime.lastReconciledAt?'info':'warn');document.getElementById('pauseQueue').disabled=config.reviewQueue.paused;document.getElementById('resumeQueue').disabled=!config.reviewQueue.paused;document.getElementById('enabled').value=String(config.enabled);document.getElementById('browserEnabled').value=String(config.browserReview.enabled);document.getElementById('projectUrl').value=config.browserReview.projectConversationUrl||'';document.getElementById('debounce').value=config.browserReview.reviewDebounceMs;document.getElementById('activeInterval').value=config.reconciliation.activeIntervalMs;document.getElementById('idleInterval').value=config.reconciliation.idleIntervalMs;document.getElementById('maxAttempts').value=config.browserReview.maxSubmissionAttempts;document.getElementById('allowMerge').value=String(config.githubActions.allowChatGPTMerge);document.getElementById('verifyClosure').value=String(config.githubActions.verifyIssueClosure);document.getElementById('closureFallback').value=String(config.githubActions.allowPaseoIssueClosureFallback);document.getElementById('prompt').value=config.browserReview.reviewPromptTemplate;document.getElementById('browserStatus').innerHTML='<span>Library: <strong>'+e(browser.library.installed?'Installed':'Missing')+'</strong></span><span>Profile: <strong>'+e(browser.profile.profileExists?'Ready':'Missing')+'</strong></span><span>Profile lock: <strong>'+e(browser.profile.locked?'In use':'Available')+'</strong></span><span>Global conversation: <strong>'+e(data.globalConversationUrl||'Not configured')+'</strong></span>';document.getElementById('history').innerHTML=data.history.length?data.history.map(event=>'<div class="event"><time>'+e(event.timestamp)+'</time><div><strong>'+e(event.entityType)+' · '+e(event.newState||event.reason)+'</strong></div><div class="muted">'+e(event.reason)+' · '+e(event.actor)+(event.sha?' · '+e(short(event.sha)):'')+(event.error?' · '+e(event.error):'')+'</div></div>').join(''):'<div class="empty">No history.</div>'}
async function refresh(){try{data=await api('/api/pr-reviews/status');render()}catch(error){toast(error.message,true)}}
async function saveSettings(){await post('/api/pr-reviews/config',{enabled:document.getElementById('enabled').value==='true',browserReview:{enabled:document.getElementById('browserEnabled').value==='true',projectConversationUrl:document.getElementById('projectUrl').value||null,reviewPromptTemplate:document.getElementById('prompt').value,reviewPromptVersion:data.config.browserReview.reviewPromptVersion,reviewDebounceMs:Number(document.getElementById('debounce').value),maxSubmissionAttempts:Number(document.getElementById('maxAttempts').value)},reconciliation:{enabled:true,activeIntervalMs:Number(document.getElementById('activeInterval').value),idleIntervalMs:Number(document.getElementById('idleInterval').value)},githubActions:{allowChatGPTMerge:document.getElementById('allowMerge').value==='true',verifyIssueClosure:document.getElementById('verifyClosure').value==='true',allowPaseoIssueClosureFallback:document.getElementById('closureFallback').value==='true'}})}
function openOverride(id,current){document.getElementById('overrideManagedId').value=id;document.getElementById('overrideUrl').value=current||'';document.getElementById('overrideDialog').showModal()}async function submitOverride(){const id=document.getElementById('overrideManagedId').value;const url=document.getElementById('overrideUrl').value.trim();closeDialog('overrideDialog');await post('/api/pr-reviews/review-now',{managedPullRequestId:id,conversationUrlOverride:url})}
function openManual(id){document.getElementById('manualManagedId').value=id;document.getElementById('manualFindings').value='';document.getElementById('manualDialog').showModal()}async function submitManual(){closeDialog('manualDialog');await post('/api/pr-reviews/manual-result',{managedPullRequestId:document.getElementById('manualManagedId').value,result:document.getElementById('manualResult').value,findings:document.getElementById('manualFindings').value})}
function openConfirm(title,phrase,path){confirmPath=path;confirmPhrase=phrase;document.getElementById('confirmTitle').textContent=title;document.getElementById('confirmText').textContent='Type '+phrase+' to continue.';const input=document.getElementById('confirmInput');input.value='';document.getElementById('confirmButton').disabled=true;input.oninput=()=>document.getElementById('confirmButton').disabled=input.value!==phrase;document.getElementById('confirmButton').onclick=async()=>{closeDialog('confirmDialog');await post(confirmPath)};document.getElementById('confirmDialog').showModal();input.focus()}
refresh();setInterval(refresh,15000);
</script>
</body>
</html>`;
}
